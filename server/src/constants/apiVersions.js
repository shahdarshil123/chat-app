export const MESSAGE_API_VERSION_ENUM = Object.freeze({
  V1: "v1",
  V2: "v2",
});

export const USER_API_VERSION_ENUM = Object.freeze({
  V1: "v1",
});

export const CONVERSATION_API_VERSION_ENUM = Object.freeze({
  V1: "v1",
});

export const AUTH_API_VERSION_ENUM = Object.freeze({
  V1: "v1",
});